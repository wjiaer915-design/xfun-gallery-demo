# X-FUN Gallery Demo

React + Tailwind CSS + Framer Motion implementation of the provided gallery screenshot.

## Run

```bash
npm install
npm run dev
```

## Gallery Images

Gallery content is isolated in `src/galleryData.js`. The matched source images are stored in `public/images`.

The original screenshot has been copied to `public/reference-ip-design.png` for visual reference.
